version: '3'

services:
  server:
    build:
      context: .
      dockerfile: Dockerfile
    command: npm start
    env_file:
      - .env
    environment:
      NODE_ENV: ${NODE_ENV}
      PORT: ${PORT}
      DATABASE_URI: ${DATABASE_URI}
    ports:
      - 80:5000

